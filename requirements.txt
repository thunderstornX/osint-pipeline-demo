# OSINT Pipeline Demo -- pinned dependencies.
#
# The pipeline runs purely on aiohttp + asyncpg.  Selenium is optional
# (only loaded when a source is configured to use the WebDriver pool).
#
# Pinned for reproducibility across CI runs and benchmark invocations.

# -- HTTP + async core --------------------------------------------------------
aiohttp==3.13.5
asyncpg==0.31.0

# -- Configuration -------------------------------------------------------------
pyyaml==6.0.3
pydantic==2.10.4
pydantic-settings==2.7.0

# -- Optional Selenium pool ----------------------------------------------------
selenium==4.43.0
webdriver-manager==4.0.2

# -- Test runner ---------------------------------------------------------------
pytest==9.0.3
pytest-asyncio==1.3.0
aioresponses==0.7.8
